# ── API URLs ──────────────────────────────────────────────
# .env          → development (used by `expo start`)
# .env.production → production (used by `npm run build:android`)

EXPO_PUBLIC_API_URL=http://192.168.100.3:3000/api
EXPO_PUBLIC_SOCKET_URL=http://192.168.100.3:3000

# ── Mapbox ────────────────────────────────────────────────
# Public access token (pk.*) — read at runtime by Mapbox.setAccessToken
EXPO_PUBLIC_MAPBOX_TOKEN=pk.your_public_token_here
# Secret download token (sk.*) — used by the @rnmapbox/maps Expo plugin during prebuild
RNMAPBOX_MAPS_DOWNLOAD_TOKEN=sk.your_download_token_here

# ── Google ────────────────────────────────────────────────
EXPO_PUBLIC_GOOGLE_WEB_CLIENT_ID=your_web_client_id_here
EXPO_PUBLIC_GOOGLE_ANDROID_CLIENT_ID=your_android_client_id_here

# ── Sentry ────────────────────────────────────────────────
EXPO_PUBLIC_SENTRY_DSN=your_sentry_dsn_here
